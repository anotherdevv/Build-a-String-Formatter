# Build-a-String-Formatter
In this workshop, I will practice on working with various string methods used for formatting strings. Such as: trim(), trimStart(), trimEnd(), toUpperCase(), and toLowerCase() methods. 
